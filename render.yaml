service:
  -type: web
  name: ml-flask-api
  env: python 
  plan: free
  buildcommands: "pip install -r requirements.txt"
  startcommand: "gunicorn app:app --bind 0.0.0.0$PORT"
  autoDeploy: false  # we'll triggger deploys via Github Actions

  